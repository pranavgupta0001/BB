package com.circle.prana.bb.Currency;

import java.util.ArrayList;
import java.util.HashMap;
import java.util.Map;

public class Customer {
    /*
    private String fromName, toName;
    private Map<String, String> senderTransactions;

    public Customer(String fromName,String toName, double initialAmount) {
        this.fromName = fromName;
        this.toName = toName;
        this.senderTransactions = new HashMap<>();
        addTransaction(initialAmount);
    }

    public void addTransaction(double amount) {
        this.senderTransactions.add(amount);
    }

    public String getName() {
        return name;
    }

    public ArrayList<Double> getTransactions() {
        return senderTransactions;
    }
    */
}
